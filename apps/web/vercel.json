{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @al-souq/db generate && pnpm --filter @al-souq/db setup:prod && next build",
  "crons": [
    {
      "path": "/api/cron/release-reservations",
      "schedule": "*/15 * * * *"
    }
  ]
}
