# V2_NOTES — مؤجّلاتٌ خارج نطاق النسخة النهائيّة الحاليّة

كلّ فكرة تحسينٍ خطرت أثناء تنفيذ الشاشات العشر — مكتوبةٌ هنا ولم تُنفَّذ (التزاماً بسياج النطاق).

## الصور
- **صورٌ حقيقيّة (royalty-free):** تعذّر تنزيلها آلياً — مزوّد الخروج في بيئة التطوير
  يحجب مضيفات الصور (Unsplash/Pexels ترجع `403`). الحاليّ: بديلٌ مُوحّد العلامة عبر
  `/api/ph`. عند توفّر مضيفٍ مسموح أو حزمة صورٍ من المالك، تُوضع في `public/images/`
  ويُحدّث ربطها في البذرة (الفتحات جاهزة: صورة منتج، شعار، غلاف).

## المحتوى التحريريّ
- **نظام مقالات (CMS):** «مقال/نصيحة اليوم» حاليّاً محتوىً ثابتٌ في `src/lib/articles.ts`.
  V2: نموذج `Article` في القاعدة + إدارةٌ من لوحة الأدمن + جدولةٌ يوميّة.

## الاكتشاف والبيانات
- **تدوير «اكتشف اليوم» يوميّاً بمُدخلٍ زمنيّ (seed) ثابت لليوم** بدل ترتيب المحرّك العام.
- **مبيعات ٧ أيام حقيقيّة** بدل `soldCount` التراكميّ في بعض الإشارات.
- **تخصيص التوصيات** حسب سجلّ المستخدم (حاليّاً عامّة لكلّ زائر).

## عناصر الواجهة
- **هياكل تحميل (skeletons) لكلّ شاشةٍ على حدة** بدل `app/loading.tsx` العامّ.
- **صور مصغّرة حقيقيّة في «نبض السوق»** (حاليّاً أيقونةُ نوعٍ ملوّنة لكلّ حدث).
- **شارة التوفّر في صفحات البحث/الفئة/المفضّلة** (مغطّاة الآن في الاكتشاف والكتالوج
  والمتجر؛ تُعمّم على بقيّة الأسطح).
- **ترتيب «قريب منك» الأوّليّ من الخادم** بموقعٍ تقريبيّ (IP) قبل منح إذن GPS.

## الخريطة
- **تجميع الدبابيس (clustering)** عند كثافة المتاجر.
- **نطاقُ بحثٍ على الخريطة** («ابحث في هذه المنطقة»).

## أداء
- **ISR/تخزين مؤقّت** للصفحات القابلة (حاليّاً `force-dynamic` لعزل المحافظة).
- **`next/image` بتحسينٍ كامل** بعد ضبط `NEXT_PUBLIC_IMAGE_HOST` لمضيف تخزينٍ حقيقيّ.
