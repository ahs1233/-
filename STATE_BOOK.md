# دفتر الحالات — واجهة «السوگ» (الشاشات العشر)

مرجعُ تسليمٍ للمطوّرين: لكلّ شاشة مسارُها، ومصدرُ بياناتها (الراوتر/الإجراء في tRPC)،
وسلوكُ نقراتها الرئيسيّة، وحالاتُها الأربع (ممتلئة/تحميل/فارغة/خطأ).

> **مصدر البيانات موحّد:** كلّ الشاشات تقرأ من قاعدة البيانات عبر tRPC فقط. لا بيانات
> ثابتة داخل المكوّنات (عدا المحتوى التحريريّ في `src/lib/articles.ts`، مؤقّتاً).
> البذرة (`packages/db/prisma/seed.ts`) هي مصدر الحقيقة، وتُشغَّل على Vercel ضمن
> `setup:prod`.

## الحالات المشتركة (طبقة التطبيق)
| الحالة | الآليّة |
|---|---|
| **تحميل** | `app/loading.tsx` (هيكلٌ عامّ) يظهر أثناء تصيير الخوادم؛ الصفحات `force-dynamic`. |
| **خطأ** | `app/error.tsx` + `app/global-error.tsx` (حدود أخطاء) — وكلّ صفحة تلفّ جلب البيانات بـ `try/catch` فتعرض حالةً فارغةً بدل الانهيار. |
| **غير موجود** | `app/not-found.tsx` (مثلاً سوق/متجر/مقال غير موجود عبر `notFound()`). |
| **عناصر مشتركة** | شريط سفليّ ثابت · حبّة المحافظة «بغداد ▾» · سهم رجوع في كلّ شاشةٍ فرعيّة (`SectionPageHeader`). |

---

## الشاشات

### ١ — الرئيسية · `/`
- **البيانات:** `discovery.cityStats` · `discovery.homeExtras` (نبض) · `appearance.content` (إعلانات) · `market.list` · `discovery.home` (أقسام) · `discovery.marketStores` · `auth.me` (اسم التحيّة).
- **المكوّنات:** `CityHero` (تحيّة + ٤ أرقام حيّة) · شريط بحث → `/search` · `MarketShortcuts` → `/markets` · نبض · عروض → `/offers` · متاجر مقترحة · الأكثر مبيعاً → `/best-selling` · وصل حديثاً → `/new-arrivals` · قريب منك → `/nearby` · متاجر جديدة → `/new-stores` · اكتشف → `/discover`.
- **النقرات:** كلّ عنوان قسمٍ يحمل «عرض الكل» يفتح صفحته. الأرقام تُشتقّ من نفس استعلامات صفحاتها (بوّابة الاتساق): سوق نشط=عدد الأسواق، عرض=منتجات الخصم، متجر افتتح=المتاجر الجديدة، منتج وصل=منتجات ٧ أيام.
- **فارغة:** لافتة «المتجر قيد التجهيز» عند تعذّر قاعدة البيانات؛ كلّ قسمٍ يختفي إن خلا.

### ٢ — اختيار السوق · `/markets`
- **البيانات:** `market.list`.
- **النقرات:** كلّ بطاقة → `/market/[slug]`؛ الأسواق `soon` تحمل شارة «قريباً».
- **فارغة:** «لا توجد أسواق متاحة بعد».

### ٣ — نبض السوق · `/pulse`
- **البيانات:** `discovery.homeExtras.pulse` (أحداثٌ مشتقّة من DB بأزمنةٍ نسبيّة من `createdAt`).
- **النقرات:** شرائح تصفية (الكل/متاجر/عروض/منتجات/أخبار) — تصفيةٌ في المتصفّح حسب نوع الحدث.
- **فارغة:** «لا يوجد نشاطٌ في هذا التصنيف بعد».

### ٤ — العروض اليوميّة · `/offers`
- **البيانات:** `discovery.offers` (منتجاتٌ لها `compareAtPrice` > السعر) · `appearance.content`.
- **المكوّنات:** لافتة ذهبيّة «خصومات تصل إلى X%» (X = أكبر خصمٍ فعليّ) · شرائح فئات (`FilteredProducts`) · بطاقاتٌ بنسبة الخصم والسعر المشطوب.
- **فارغة:** «لا عروض فعّالة الآن».

### ٥ — المتاجر المميّزة · `/stores`
- **البيانات:** `catalog.stores` (يعيد: التوثيق، الفئة، صورة الغلاف، عدّاد المبيعات، تاريخ الإنشاء).
- **النقرات:** فرزٌ في المتصفّح (المميّزة/الأعلى تقييماً/الأكثر مبيعاً/حديثة) · «زيارة» → `/store/[slug]`.
- **عناصر:** شعار مصمّم · علامة ✓ ذهبيّة (للموثّق فقط) · الفئة · التقييم (العدد).
- **فارغة:** احتياط «متاجر بقيّة العراق» إن خلت المحافظة، ثمّ «لا توجد متاجر بعد».

### ٦ — الأكثر مبيعاً · `/best-selling`
- **البيانات:** `discovery.home` → قسم `best_selling` (فرز `soldCount` تنازليّاً، متوفّر فقط).
- **المكوّنات:** `FilteredProducts` (نسق مرقّم) — أرقام ذهبيّة للمراكز ١–٣ · «X عملية شراء» لكلّ عنصر · شرائح فئات.
- **فارغة:** «لا توجد بيانات مبيعات بعد».

### ٧ — وصل حديثاً · `/new-arrivals`
- **البيانات:** `discovery.home` → قسم `new` (نافذة ٧ أيام).
- **المكوّنات:** شبكة عمودين · شارة «جديد» · شرائح فئات · شارة الخصم/التوفّر.
- **فارغة:** «لا توجد منتجات جديدة بعد».

### ٨ — قريب منك · `/nearby`
- **البيانات:** `discovery.nearbyStores` (متاجر لها إحداثيّات).
- **المكوّنات:** خريطة Leaflet ببلاطات CartoDB الداكنة + دبابيس ذهبيّة (`NearbyMap`) · زرّ GPS · قائمةٌ مرتّبةٌ بالمسافة (haversine من موقع المتصفّح).
- **النقرات:** «فعّل موقعي» يطلب GPS ويعيد الترتيب · كلّ صفّ → `/store/[slug]`.
- **فارغة:** «لا توجد متاجر بموقعٍ محدّد بعد».
- **ملاحظة:** موقع المتجر يُضبط من لوحة التاجر (`/vendor/settings`) بمُنتقي خريطة.

### ٩ — المتاجر الجديدة · `/new-stores`
- **البيانات:** `discovery.home` → قسم `new_stores` (خلال ١٤ يوماً + ٥ منتجاتٍ فأكثر).
- **المكوّنات:** شبكة بطاقات (`StoreRailCard`) بصورة غلاف + شعار + تقييم.
- **فارغة:** «لا توجد متاجر جديدة بعد».

### ١٠ — اكتشف اليوم · `/discover`
- **البيانات:** `discovery.home` → قسم `today` (تشكيلةٌ متجدّدة) · `src/lib/articles` (محتوى تحريريّ بذرة).
- **المكوّنات:** بطاقة «منتج اليوم» الكبيرة (السعر/الخصم + «اكتشف الآن») · شبكة اكتشافٍ · بطاقتا «مقال اليوم» و«نصيحة اليوم» → `/article/[slug]`.
- **فارغة:** «لا يوجد اكتشافٌ اليوم بعد».

---

## عناصر الثقة (عبر التطبيق)
- **التوفّر:** شارة «متوفّر» (نقطة خضراء) / «غير متوفّر» (رمادي + تعتيم) على بطاقة المنتج — من حقل `available` (مجموع مخزون المتغيّرات). ~١٠٪ من منتجات البذرة «غير متوفّرة».
- **التوثيق:** علامة ✓ ذهبيّة بجانب اسم المتجر الموثّق (`VendorProfile.verified`).
- **الدفع عند الاستلام:** سطرُ طمأنةٍ في صفحة المنتج والسلّة والدفع.

## الصور
- بديلٌ مُوحّد العلامة عبر `/api/ph` (SVG داكن + رمز الفئة + الاسم) لكلّ منتج/متجر، يُستبدَل بصورةٍ حقيقيّة عند الرفع من لوحة التاجر أو بوضع ملفٍّ في `public/`.
