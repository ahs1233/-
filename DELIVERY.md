# تسليم «السوگ» — النسخة النهائيّة للواجهة (الشاشات العشر)

منصّةُ تجارةٍ متعدّدة البائعين للعراق، عربيّة RTL، بثيمٍ ليليّ نيليّ/ذهبيّ.
هذا الملفّ ملخّصُ التسليم؛ للتفاصيل التشغيليّة راجع `STATE_BOOK.md`، وللمؤجّلات `V2_NOTES.md`.

- **المستودع/الفرع:** `ahs1233/-` · `claude/al-souq-ecommerce-build-ym3pze`
- **التقنيّات:** Next.js 14 (App Router) · TypeScript · tRPC · Prisma/PostgreSQL · Tailwind · Leaflet — منشور على Vercel.
- **الحالة:** بناء الإنتاج ناجح · ٢٠/٢٠ اختبار · تحقّق بصريّ حيّ للشاشات العشر على ٣٩٠×٨٤٤.

---

## الشاشات العشر
| # | الشاشة | المسار | الجوهر |
|---|--------|--------|--------|
| ١ | الرئيسية | `/` | بطل المدينة (تحيّة + ٤ أرقام حيّة) · بحث · صفّ أسواق · خلاصة اكتشاف |
| ٢ | اختيار السوق | `/markets` | شبكة بوّابات الأسواق (أيقونة + اسم + وصف + «قريباً») |
| ٣ | نبض السوق | `/pulse` | خلاصةٌ مباشرة بفلاتر (الكل/متاجر/عروض/منتجات/أخبار) |
| ٤ | العروض اليوميّة | `/offers` | لافتة «خصومات تصل إلى X%» + شرائح + بطاقات الخصم |
| ٥ | المتاجر المميّزة | `/stores` | فرز (مميّزة/تقييم/مبيعات/حديثة) · توثيق ✓ · فئة · «زيارة» |
| ٦ | الأكثر مبيعاً | `/best-selling` | قائمة مرقّمة + «X عملية شراء» + شرائح فئات |
| ٧ | وصل حديثاً | `/new-arrivals` | شبكة + شارة «جديد» + شرائح فئات |
| ٨ | قريب منك | `/nearby` | خريطة Leaflet داكنة + دبابيس + ترتيبٌ بالمسافة (GPS) |
| ٩ | المتاجر الجديدة | `/new-stores` | شبكة بطاقات المتاجر المفتتحة حديثاً |
| ١٠ | اكتشف اليوم | `/discover` | منتج اليوم المميّز + «مقال/نصيحة اليوم» → `/article/[slug]` |

---

## نظام البيانات الموحّد (Canonical)
مصدرُ الحقيقة هو `packages/db/prisma/seed.ts` (يُشغَّل على Vercel ضمن `setup:prod`).
لا بيانات ثابتة داخل المكوّنات. **نفس المنتج/المتجر = نفس السعر/التقييم في كلّ الشاشات.**

- **١١ متجراً قانونيّاً** (بغداد): دار الأناقة · بيت العطور · مكتبة المتنبي · مطعم دجلة (موثّقة) · متجر القمة · أزياء البغدادية · بيت الحلويات · مكتبة النافذة (جديدة، ٥ منتجات لكلٍّ) · سوبر ماركت الزوراء · صيدلية الحياة · مطعم بيت أمي (بإحداثيّات).
- **منتجات قانونيّة** بأسعارٍ وخصوماتٍ وتقييماتٍ ثابتة، فيتطابق:
  - الأكثر مبيعاً: عطر فرنسي ← iPhone 15 Pro Max ← ساعة رجالية ← طقم صحون.
  - العروض: iPhone 15 Pro (١٬٣٥٠٬٠٠٠/١٬٦٠٠٬٠٠٠) · ساعة ذكية · ساعة رجالية · طقم صحون.
- **عدّادات بطل المدينة** تُشتقّ من نفس استعلامات صفحاتها (بوّابة الاتساق):
  سوق نشط=الأسواق · عرض=منتجات الخصم · متجر افتتح=المتاجر الجديدة · منتج وصل=منتجات ٧ أيام.

## عناصر الثقة (عبر التطبيق)
- **التوفّر:** شارة «متوفّر/غير متوفّر» من حقل `available` (~١٠٪ نافد في البذرة).
- **التوثيق:** علامة ✓ ذهبيّة (`VendorProfile.verified`) — للموثّق فقط.
- **الدفع عند الاستلام:** في صفحة المنتج والسلّة والدفع.

## نظام الصور
بديلٌ مُوحّد العلامة عبر `/api/ph` (SVG داكن + رمز الفئة + الاسم) لكلّ منتج/متجر.
يُستبدل بصورةٍ حقيقيّة عند رفعها من لوحة التاجر أو بوضع ملفٍّ في `public/`.
> الصور الحقيقيّة royalty-free مؤجّلة (سياسة شبكة بيئة التطوير تحجب مضيفات الصور) — الفتحات جاهزة.

## الحالات الحدّية
- **تحميل:** هياكل (skeletons) داكنة بنفس تخطيط كلّ شاشة (`loading.tsx` لكلّ مسار).
- **فارغة:** رسائل عربيّة مصمّمة في كلّ صفحة.
- **خطأ / غير موجود:** `app/error.tsx` · `global-error.tsx` · `not-found.tsx`.

---

## التشغيل محليّاً
```bash
pnpm install
pnpm --filter @al-souq/db generate
pnpm --filter @al-souq/db deploy   # تطبيق الهجرات
pnpm --filter @al-souq/db seed     # البذرة القانونيّة
pnpm --filter @al-souq/web dev
```
- **حساب الأدمن:** الهاتف `07700000000` (رمز OTP يظهر في التطوير).
- **النشر (Vercel):** `setup:prod` يطبّق الهجرات ثمّ يبذر تلقائياً أثناء البناء.

## نتائج التحقّق
- `pnpm --filter @al-souq/web build` → ناجح (كلّ المسارات تُبنى).
- `pnpm --filter @al-souq/api test` → ٢٠/٢٠.
- تحقّق بصريّ حيّ على ٣٩٠×٨٤٤ للشاشات العشر.
- اتساق المنتجات/المتاجر عبر الشاشات · عدّادات البطل تطابق صفحاتها.

## المؤجّل (راجع `V2_NOTES.md`)
صورٌ حقيقيّة · نظام مقالات (CMS) · صور مصغّرة في النبض · تجميع دبابيس الخريطة · ISR.
